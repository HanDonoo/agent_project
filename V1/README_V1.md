# Employee Finder Agent - V1 (旧版本)

## ⚠️ 注意

**这是旧版本的代码，已被 EC_Proj 替代。**

此目录包含原始的简单关键词匹配系统，保留用于参考和回退。

## 📦 V1 版本特性

- ✅ 简单关键词匹配
- ✅ 16 个模拟员工
- ✅ 基础 OpenWebUI 集成
- ✅ SQLite 数据库
- ✅ FastAPI REST API

## 🔄 迁移到 EC_Proj

如果你想使用新版本，请参考：

```bash
cd ../EC_Proj
cat README.md
```

## 📁 V1 目录结构

```
V1/
├── agent/              # Agent 逻辑
│   ├── ai_agent.py
│   ├── employee_finder_agent.py
│   ├── router.py
│   └── tools.py
├── api/                # FastAPI 服务器
│   └── main.py
├── database/           # 数据库管理
│   ├── db_manager.py
│   ├── models.py
│   └── schema.sql
├── scripts/            # 工具脚本
│   ├── create_mock_data.py
│   ├── import_employees.py
│   └── start_server.py
├── tests/              # 测试
│   ├── test_agent.py
│   └── test_router.py
├── data/               # 数据文件
│   └── employee_directory.db
├── config.py           # 配置
├── requirements.txt    # 依赖
└── README.md           # 原始文档
```

## 🚀 如何运行 V1 (不推荐)

如果你确实需要运行旧版本：

```bash
cd V1

# 安装依赖
pip install -r requirements.txt

# 启动服务器
python scripts/start_server.py
```

## 🆕 为什么升级到 EC_Proj？

| 改进 | V1 | EC_Proj |
|------|-----|---------|
| **AI 能力** | ❌ 无 | ✅ Ollama LLM |
| **技能系统** | 简单标签 | 4 级熟练度 |
| **员工数量** | 16 | 200 |
| **匹配精度** | 低 | 高 |
| **复杂查询** | ❌ 不支持 | ✅ 支持 |
| **评分系统** | 简单 | 加权算法 |

## 📚 原始文档

查看原始 README：
```bash
cat README.md
```

## 🗄️ 存档日期

2026-01-22

---

**推荐使用**: [../EC_Proj](../EC_Proj)  
**此版本状态**: 已存档，仅供参考

